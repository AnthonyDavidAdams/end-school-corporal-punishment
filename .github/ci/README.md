`validate.yml` is the GitHub Actions workflow that runs the schema validator on every pull request. It lives here rather than in `.github/workflows/` only because the token used for the first push lacked the `workflow` scope. To enable it: `git mv .github/ci/validate.yml .github/workflows/validate.yml` and push with a token that has that scope (`gh auth refresh -h github.com -s workflow`).
