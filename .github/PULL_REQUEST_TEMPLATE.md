## What this changes

<!-- one paragraph -->

## Sources

<!-- every new or changed fact, status, or figure lists its source URL here or in the file itself -->

## Checklist

- [ ] `cd tools && npm run validate` passes (CI runs the same check once `.github/ci/validate.yml` is enabled)
- [ ] Every non-`unknown` district status has `source`, `quote` and `last_verified`
- [ ] Every claim I touched has `status`, `sources` and `last_verified` updated
- [ ] No personal information about any student, and no unverified allegations about any named person
- [ ] If an agent produced this PR: the agent name and the prompt or skill used are noted below

## Agent disclosure (if applicable)

<!-- e.g. "Claude Code, skill district-policy-scan v1, run by @handle; every source URL was opened and the quote copied verbatim" -->
